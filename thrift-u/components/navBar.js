'use client'

import FireData from '../firebase/clientApp'
import {doc, getDoc} from '@firebase/firestore'

const NavBar = () => {

    return (
        true //This is a filler for a modular NAV Bar that changes depending on login status
    )
}

export default NavBar